apiVersion: v1
kind: Service
metadata:
  name: {{ include "dropshipper-ui.fullname" . }}
  labels:
    app: {{ include "dropshipper-ui.name" . }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    app: {{ include "dropshipper-ui.name" . }}

